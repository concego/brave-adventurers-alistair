name: Build Android APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Install Android SDK components
        run: |
          export ANDROID_SDK_ROOT="${ANDROID_SDK_ROOT:-/usr/local/lib/android/sdk}"
          export PATH="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$ANDROID_SDK_ROOT/platform-tools:$PATH"
          yes | sdkmanager --licenses > /dev/null 2>&1 || true
          sdkmanager "build-tools;34.0.0" "platforms;android-34" "ndk;23.2.8568313"
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT" >> $GITHUB_ENV
          echo "ANDROID_NDK_ROOT=$ANDROID_SDK_ROOT/ndk/23.2.8568313" >> $GITHUB_ENV

      - name: Install Godot 4.3
        run: |
          wget -q "https://github.com/godotengine/godot/releases/download/4.3-stable/Godot_v4.3-stable_linux.x86_64.zip" -O godot.zip
          unzip -q godot.zip
          chmod +x Godot_v4.3-stable_linux.x86_64
          sudo mv Godot_v4.3-stable_linux.x86_64 /usr/local/bin/godot
          echo "Godot $(godot --version)"

      - name: Install Android export templates
        run: |
          TEMPLATES_DIR="$HOME/.local/share/godot/export_templates/4.3.stable"
          mkdir -p "$TEMPLATES_DIR"
          wget -q "https://github.com/godotengine/godot/releases/download/4.3-stable/Godot_v4.3-stable_export_templates.tpz" -O templates.tpz
          unzip -q templates.tpz -d /tmp/templates_extracted
          # Mover todos os templates para a pasta correta
          mv /tmp/templates_extracted/templates/* "$TEMPLATES_DIR/"
          echo "Templates instalados:"
          ls "$TEMPLATES_DIR/" | grep android

      - name: Configure Godot editor settings
        run: |
          EDITOR_SETTINGS_DIR="$HOME/.config/godot"
          mkdir -p "$EDITOR_SETTINGS_DIR"
          cat > "$EDITOR_SETTINGS_DIR/editor_settings-4.tres" << EOF
          [gd_resource type="EditorSettings" format=3]
          [resource]
          export/android/android_sdk_path = "$ANDROID_SDK_ROOT"
          EOF
          echo "Editor settings configurado com SDK: $ANDROID_SDK_ROOT"

      - name: Pre-heat project (importar assets)
        run: |
          # ESSENCIAL: gera o cache .godot/ antes do export
          # Sem isso o Godot trava ao tentar importar assets durante o export
          echo "Aquecendo projeto (importando assets)..."
          godot --headless --editor --quit || true
          echo "Cache .godot/ gerado:"
          ls .godot/ 2>/dev/null || echo "(pasta .godot nao criada, pode ser normal)"

      - name: Export APK (debug)
        run: |
          mkdir -p build
          echo "Iniciando export..."
          godot --headless --verbose --export-debug "Android" build/brave-adventurers-alistair-debug.apk
          echo "Status: $?"
          ls -lh build/

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: brave-adventurers-alistair-debug
          path: build/brave-adventurers-alistair-debug.apk
          if-no-files-found: error
          retention-days: 14
